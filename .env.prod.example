# Copy this file to .env on the VPS and fill in real values.
# Do NOT commit the real .env.

# --- Public address ---
# Free pseudo-domain bound to your VPS IP via sslip.io (dashes instead of dots).
# Example: VPS IP 203.0.113.5  ->  203-0-113-5.sslip.io
SITE_ADDRESS=203-0-113-5.sslip.io
APP_BASE_URL=https://203-0-113-5.sslip.io

# --- PostgreSQL ---
POSTGRES_DB=ordertracking
POSTGRES_USER=ordertracking
POSTGRES_PASSWORD=change_me_strong_password

# --- JWT (min 32 characters) ---
JWT_SECRET=change_me_to_a_random_string_at_least_32_chars

# --- MinIO ---
MINIO_ROOT_USER=change_me_minio_user
MINIO_ROOT_PASSWORD=change_me_strong_password

# --- First admin (seeded on empty DB) ---
SEED_ADMIN_LOGIN=admin
SEED_ADMIN_PASSWORD=change_me_admin_password

# --- Telegram Login Widget (BotFather) ---
# Leave empty to disable Telegram login / bind.
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
